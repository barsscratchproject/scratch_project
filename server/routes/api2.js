const express = require('express');
const userController = require('');// /controllers/userController2.js

const router = express.Router();

router.get('/',
    //what middleware shall we serve to root
    cardController.
)